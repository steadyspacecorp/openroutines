name: "{{AGENT_NAME}}"
description: "{{JOB_DESCRIPTION}}"
owner:
  name: "{{OWNER_NAME}}"
  email: "{{OWNER_EMAIL}}"
timezone: "{{TIMEZONE}}"
defaults:
  model: "{{DEFAULT_MODEL}}"
  timeout: 5m
memory:
  retention: 30d
